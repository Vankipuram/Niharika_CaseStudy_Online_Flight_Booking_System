import { FlightBookRequest } from './flight-book-request';

describe('FlightBookRequest', () => {
  it('should create an instance', () => {
    expect(new FlightBookRequest()).toBeTruthy();
  });
});
